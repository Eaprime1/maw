# sandbox (working name)

Holding pool for a contamination flush. When something is contaminating a
pool, the pool's contents land here as `<prima-clock>_<pool>/` so the rest of
the Maw keeps working. Clean items return to their pool; the problem item is
worked on here until it can re-enter the stream. Nothing is erased. Routine:
`../../ARRIVAL.md`.
